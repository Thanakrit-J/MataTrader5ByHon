### Windows
# Windows thumbnail cache files
Thumbs.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows shortcuts
*.lnk

# Python virtual environment
venv/

# Python caches
__pycache__/
*.pyc
.pytest_cache/

# Generated datasets (Parquet + manifest, spec §4.3) — reproducible, not committed
# Anchored to repo root so it does not match the mt5gold/data/ source package.
/data/

# Design diagrams (visual aids — kept local, not version-controlled)
docs/diagrams/